specVersion: "v1"
serviceId: "voice-catalyst"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "voice-catalyst-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
